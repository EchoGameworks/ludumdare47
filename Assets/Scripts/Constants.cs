using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Constants {
    public enum SkillTypes { Normal, Ice, Fire, Grass, Metal, Electricity, None }
    public enum UnitTypes { Player, Boss, Minion }
}
